# 语音交互系统 — 完整流程图

```mermaid
flowchart TD
    subgraph 启动["🔌 装置启动"]
        A1["上电 / 复位"] --> A2["初始化全部外设"]
    end

    subgraph 外设初始化["外设初始化"]
        B1["INMP441 麦克风<br/>I2S 音频采集"]
        B2["TJC 串口屏<br/>UART6 显示交互"]
        B3["AS608 指纹模块<br/>UART2 指纹采集"]
        B4["PN532 NFC 模块<br/>I2C2 读卡识别"]
        B5["ESP32 WiFi<br/>UART3 云端通信"]
        B6["MFCC + DTW<br/>语音识别引擎"]
        B7["Speaker Verify<br/>声纹验证"]
    end

    A2 --> B1
    A2 --> B2
    A2 --> B3
    A2 --> B4
    A2 --> B5
    A2 --> B6
    A2 --> B7

    B1 --> C1["✅ 初始化完成<br/>进入主循环"]

    subgraph 主循环["🔄 主循环 (非阻塞轮询)"]
        direction TB
        D1["dispatch_poll()<br/>NFC 后台扫描 + 任务调度"]
        D2["finger_search()<br/>指纹后台扫描"]
        D3["音频采集 → VAD<br/>语音活动检测"]
        D4["MFCC 特征提取 + DTW 匹配<br/>语音指令识别"]
        D5["Speaker Verify<br/>声纹验证 (鉴权)"]
    end

    C1 --> D1
    D1 --> D2
    D2 --> D3
    D3 --> D4
    D4 --> D5
    D5 --> D1

    subgraph 麦克风🎤["🎤 麦克风 — 语音指令"]
        E1["说「打卡」DA_KA"] --> E1a["声纹验证通过 →<br/>串口屏显示: 打卡<br/>等待指纹打卡"]
        E2["说「可乐」KE_LE"] --> E2a["串口屏显示: 可乐<br/>指令确认"]
        E3["说「雪碧」XUE_BI"] --> E3a["串口屏显示: 雪碧<br/>指令确认"]
        E4["说「牛奶」NIU_NAI"] --> E4a["串口屏显示: 牛奶<br/>指令确认"]
        E5["说「信息」XIN_XI"] --> E5a["串口屏显示: 信息<br/>待定操作"]
    end

    subgraph 串口屏📺["📺 串口屏 — 实时交互"]
        F1["实时显示识别结果<br/>「XX 打卡成功」"]
        F2["显示刷卡信息<br/>卡号 / 用户身份"]
        F3["添加指纹<br/>录入新指纹模板"]
        F4["删除指纹<br/>移除指定指纹"]
        F5["系统参数设置<br/>安全等级 / 时间同步"]
        F6["显示日期时间<br/>ESP32 云端同步"]
    end

    subgraph 指纹👆["👆 指纹模块 — 身份认证"]
        G1["手指放入感应区"] --> G2["PS_Sta 检测<br/>非阻塞消抖"]
        G2 --> G3["PS_GetImage<br/>采集指纹图像"]
        G3 --> G4["PS_GenChar<br/>生成指纹特征"]
        G4 --> G5["PS_HighSpeedSearch<br/>搜索指纹库"]
        G5 --> G6{匹配结果?}
        G6 -->|"✅ 匹配成功"| G7["确认身份<br/>自动考勤打卡<br/>ESP32 上报"]
        G6 -->|"❌ 匹配失败"| G8["验证失败<br/>串口屏提示<br/>冷却 1s"]
    end

    subgraph NFC📇["📇 NFC 模块 — 刷卡识别"]
        H1["卡片靠近感应区"] --> H2["PN532_connect()<br/>持续扫描 (非阻塞)"]
        H2 --> H3["双向防抖<br/>NFC_DB=10 次确认"]
        H3 --> H4["读取卡 UID<br/>ISO14443A"]
        H4 --> H5{授权卡片?}
        H5 -->|"✅ 授权卡"| H6["显示卡信息<br/>自动考勤打卡<br/>ESP32 上报"]
        H5 -->|"❌ 未授权"| H7["拒绝访问<br/>串口屏提示"]
    end

    %% 模块间交互
    E1a -.-> G1
    G7 -.-> F1
    H6 -.-> F1
    F3 -.->|"录入成功"| G1
    H6 -.-> F2
    B5 -.->|"@1~@6 考勤 / @WIFI 配网"| F6
    E2a -.->|"命令分发"| F1
    E3a -.->|"命令分发"| F1
    E4a -.->|"命令分发"| F1

    %% 声纹验证
    D5 --> E1
    D5 --> E2
    D5 --> E3
    D5 --> E4
    D5 --> E5
```

---

## 交互流程说明

```
┌──────────────────────────────────────────────────────────┐
│                      装置启动                             │
│   INMP441 → MFCC Engine → DTW Templates → Speaker Verify │
│   TJC 屏 ← UART6  │  AS608 ← UART2  │  PN532 ← I2C2     │
│   ESP32 ← UART3 (WiFi / MQTT 云端)                       │
└──────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────┐
│              主循环 (非阻塞, 每圈 ~60ms)                   │
│                                                          │
│  dispatch_poll()    ← NFC 扫描 + 任务状态机               │
│  finger_search()    ← 指纹后台持续扫描                    │
│  inmp441 → VAD → MFCC → DTW → 识别指令                   │
│  speaker_verify()   ← 声纹鉴权 (仅授权用户可用)           │
└──────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  🎤 麦克风   │  │  👆 指纹     │  │  📇 NFC      │
│  语音指令    │  │  身份认证    │  │  刷卡识别    │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ 打卡 → 串口屏 │  │ 匹配 → 打卡  │  │ 授权 → 打卡  │
│ 可乐 → 串口屏 │  │ 失败 → 拒绝  │  │ 拒绝 → 提示  │
│ 雪碧 → 串口屏 │  │              │  │              │
│ 牛奶 → 串口屏 │  │              │  │              │
│ 信息 → 待定  │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────┐
│              📺 串口屏 — 统一信息展示                      │
│                                                          │
│  • 实时显示: 识别结果 / 打卡记录 / 刷卡信息               │
│  • 指纹管理: 添加 / 删除指纹模板                          │
│  • 系统设置: 安全等级 / 时间同步 / WiFi 配置              │
│  • 云端同步: ESP32 MQTT 时间 + 考勤数据上报               │
└──────────────────────────────────────────────────────────┘
```

---

## 四个外设职责总结

| 外设 | 接口 | 角色 | 触发方式 | 输出 |
|------|------|------|---------|------|
| **麦克风** INMP441 | I2S1 | 语音指令输入 | 说出指令词 | 指令 class → 串口屏显示 |
| **指纹** AS608 | UART2 | 身份认证 | 手指按压 | 匹配 ID → 打卡 |
| **NFC** PN532 | I2C2 | 刷卡识别 | 卡片靠近 | 卡 UID → 打卡 |
| **串口屏** TJC | UART6 | 信息展示+管理 | 触摸/自动 | 显示、录入、设置 |
| **WiFi** ESP32 | UART3 | 云端通信 | 事件触发 | MQTT 考勤上报 |

> **数据流向**: 麦克风 → 串口屏显示 ｜ 指纹/NFC → 识别/认证 → 串口屏 + ESP32 考勤上报






天问语音模块
当语音模块接收到：
接收到字符串@0\r\n时语音输出：你好，我叫小白，很高兴为您服务！
接收到字符串@1\r\n时语音输出：员工张三已打卡，积分加十
接收到字符串@2\r\n时语音输出：员工李四已打卡，积分加十
接收到字符串@3\r\n时语音输出：员工王五已打卡，积分加十
接收到字符串@4\r\n时语音输出：打卡失败，请重试
接收到字符串@5\r\n时语音输出：你好，你的信息未录入此系统，取物失败，如再次强制取物则联系主管！
接收到字符串@6\r\n时语音输出：您的身份记录已失效！如想操作请再次认证身份
接收到字符串@7\r\n时语音输出：你已取走可乐，减5积分
接收到字符串@8\r\n时语音输出：你已取走宏宝莱，减5积分
接收到字符串@9\r\n时语音输出：你已取走牛奶，减10积分
接收到字符串@10\r\n时语音输出：您的积分不足，请充值或明天再来！
接收到字符串@11\r\n时语音输出：库存无货，请下次再来
接收到字符串@12\r\n时语音输出：好的，已为你打开柜门，请取物！
接收到字符串@13\r\n时语音输出：好的，您的信息已显示！

帮我添加补充以下语音功能提醒
当我们连接上网络时发送@0\r\n，启动小白，当用指纹，NFC，或者语音授权人指令打卡时发送对应的@1，2，3给语音模块提示打卡，但指纹，NFC打卡失败时发送@4
当没有获得授权人身份却被取走物品时@5，当授权人手动退出或者超时退出身份授权时则发@6提醒；当授权身份人取走物品时发送对应的@7，8，9
当授权身份人积分余额不足交互该物品时则提醒@10；当发送柜里面没有一件物品时则发@11，可以在每次触发库存刷新时检测发送，不需要一直发送
当授权语音指令收到“牛奶，可乐，宏宝莱”的长或短句指令时发送@12；当授权语音指令收到“信息”的长或短句指令时发送@13；




你的现象	调哪个	方向
自己的指令识别不到	TH_BASE	调大
聊天/无关话语被误识别	TH_BASE	调小
触发成错误的指令	DTW_MARGIN	调大
别人说你的指令也能触发	SPK_SIM_THRESHOLD	调大
自己总被声纹拒绝	SPK_SIM_THRESHOLD	调小
说了没反应（不触发录音）	VAD 数值	减小
没说话也误触发	VAD 数值	增大
"大白"喊不醒	WAKE_THRESHOLD	调大
没喊"大白"也误唤醒	WAKE_THRESHOLD	调小




