现在需要优化的下面几个点

接下来我们完善指纹设置

首先接收到串口屏数据if (u(1) == 0x13) // 刷新指纹数量给串口屏。指纹数量显示用t50控件
 if (u(1) == 0x14) // 添加指纹
 执行添加指纹函数。然后控件t51是显示添加指纹的状态（添加中，确认中，添加完成，添加失败）
if (u(1) == 0x15) // 删除全部指纹
以上添加或删除后都需要刷新指纹数量t50

if (u(1) == 0x16) // 删除第一个指纹
if (u(1) == 0x17) // 删除第二个指纹
if (u(1) == 0x18) // 删除第三个指纹
如果该位置没有指纹数据的话删除不做影响，不崩溃



关于NFC的设置
if (u(1) == 0x19)进入NFC读取页，将读取到的NFC卡的UID上传到控件实时t52显示UID
if (u(1) == 0x20)退出NFC读取页，不用t52显示


关于指令参数
有5个
VAD 触发灵敏度   数值显示t70            if (u(1) == 0x20)数值加100；if (u(1) == 0x21)数值减100
TH_BASE（指令灵敏度） 数值显示t71  if(u(1) == 0x22)数值加0.02；if (u(1) == 0x23)数值减0.02
DTW_MARGIN（唯一性/抗误识别）数值显示t72 if(u(1) == 0x24)数值加0.02；if (u(1) == 0x25)数值减0.02
WAKE_THRESHOLD（唤醒灵敏度）数值显示t73 if(u(1) == 0x26)数值加0.02；if (u(1) == 0x27)数值减0.02
SPK_SIM_THRESHOLD（声纹严格度数值显示t74 if(u(1) == 0x28)数值加0.02；if (u(1) == 0x29)数值减0.02
然后数值显示可以在进入参数设置界面if(u(1) == 0x30)//进入参数设置界面刷新一次
和在对应的参数数值变化的时候刷新该参数一次
